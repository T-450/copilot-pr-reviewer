interface:
  display_name: "Copilot Pr Reviewer"
  short_description: "Repo-specific patterns and workflows for copilot-pr-reviewer"
  default_prompt: "Use the copilot-pr-reviewer repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true